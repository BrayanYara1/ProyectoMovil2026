<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.App.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#EEEEEE">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <View
            android:id="@+id/skeletonDate"
            android:layout_width="56dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_skeleton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <View
            android:id="@+id/skeletonTitle"
            android:layout_width="120dp"
            android:layout_height="16dp"
            android:layout_marginStart="16dp"
            android:background="@drawable/bg_skeleton"
            app:layout_constraintStart_toEndOf="@id/skeletonDate"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/skeletonSub"
            android:layout_width="180dp"
            android:layout_height="12dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_skeleton"
            app:layout_constraintStart_toStartOf="@id/skeletonTitle"
            app:layout_constraintTop_toBottomOf="@id/skeletonTitle" />

        <View
            android:id="@+id/skeletonMotivo"
            android:layout_width="100dp"
            android:layout_height="12dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_skeleton"
            app:layout_constraintStart_toStartOf="@id/skeletonTitle"
            app:layout_constraintTop_toBottomOf="@id/skeletonSub" />

        <View
            android:layout_width="60dp"
            android:layout_height="14dp"
            android:background="@drawable/bg_skeleton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
